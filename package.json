{
  "name": "vehicle-transfer-module",
  "version": "1.0.0",
  "description": "Vehicle transfer management",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "frontend:dev": "npm run dev --prefix frontend",
    "backend:dev": "npm run start:dev --prefix backend",
    "dev": "concurrently \"npm run frontend:dev\" \"npm run backend:dev\"",
    "lint": "npm run lint --prefix frontend && npm run lint --prefix backend",
    "test": "npm run test --prefix frontend && npm run test --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
